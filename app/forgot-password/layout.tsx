import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Forgot Password | B1 Visa Interview AI Coach",
  description: "Reset your password for your B1 Visa Interview AI Coach account.",
};

export default function ForgotPasswordLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
} 